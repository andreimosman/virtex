#!/bin/sh

#
# RC
##########################
VTXHOME=/mosman/virtex
APPHOME={$VTXHOME}/app
BIN=${APPHOME}/bin
ETC=${APPHOME}/etc
PHP=/usr/local/bin/php

${VTXHOME}/radius/sbin/radiusd -y

. ${VTXHOME}/etc/rc.firewall

${PHP} ${VTXBIN}/vtx-spool.php -b
${PHP} ${VTXBIN}/vtx-pppoe.php -RU


